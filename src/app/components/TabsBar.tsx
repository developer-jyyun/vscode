import NavItem from "./NavItem";

export default function Tabsbar() {
  return (
    <nav className="bg-sub">
      <ul className="flex items-center flex-wrap">
        <NavItem />
      </ul>
    </nav>
  );
}
